#include<stdio.h>

typedef struct No
{
    No * next;
    int valor;
}No;

typedef struct Lista
{
    No start;
    int len;
}Lista;

No *busca()
{
    No *node=(No*)malloc(sizeof(No));

}

int main()
{
    Lista list;
}